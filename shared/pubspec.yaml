name: shared
description: Shared models and realtime simulation services for the Flutter apps.
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.0.3
  equatable: ^2.0.7
  firebase_auth: ^6.0.2
  firebase_core: ^4.1.1
  flutter_riverpod: ^3.3.1
  hive: ^2.2.3
  jitsi_meet_flutter_sdk: ^12.1.3
